version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - VITE_API_URL=http://103.215.229.163:8000
    restart: unless-stopped

# Hapus konfigurasi network karena tidak diperlukan 